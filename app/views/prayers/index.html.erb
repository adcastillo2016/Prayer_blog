
<% @prayer.each do |post|%>
<div class="home">
	<h2><%= post.title%></h2>

	<div class="message"><%= post.message%></div>
	<p class="date"><%= post.created_at.strftime("%B %d, %Y") %></p>
	<br>
	<%= link_to "Read Prayer", post %>
</div>
<% end %>
